[package]
name = "azide-feed"
version = "0.1.0"
edition = "2024"
license = "AGPL-3.0"
authors = ["Geosmin Jones"]

[dependencies]
feed-rs = "2"
futures = "0.3"
reqwest = { version = "0.12", default-features = false, features = ["gzip", "rustls-tls"] }
