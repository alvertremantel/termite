[package]
name = "azide-config"
version = "0.1.0"
edition = "2024"
license = "AGPL-3.0"

[dependencies]
jones-config = { path = "../jones-config" }
dirs.workspace = true
serde.workspace = true

[dev-dependencies]
tempfile.workspace = true
